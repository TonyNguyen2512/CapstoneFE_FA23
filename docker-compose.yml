version: '2.2'
services:

  workshop-management-system-jama-fe:
    image: workshop-management-system-jama-fe
    build: 
      context: ./
      dockerfile: Dockerfile
    container_name: workshop-management-system-jama-fe
    ports:
     - "8006:3000"
    restart: "always"
    networks:
      - nginx-network

networks:
  nginx-network:
    external:
      name: nginx-network
